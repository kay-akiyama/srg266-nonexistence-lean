/-
Copyright (c) 2026 Kay Akiyama.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Kay Akiyama

Generated by `scripts/generate_root_lattice_data.py`.
Do not edit by hand.
-/
import SRG266.Lattice.RootLattice
import SRG266.Certificates.RootLatticeData

/-!
# Kernel certificates for the irreducible root lattices of type `E₆`, `E₇`, `E₈`

One `SRG266.Lattice.RootLatticeCert` per type, each field checked by
`decide +kernel` against the generated data of
`SRG266/Certificates/RootLatticeData.lean`, and the four consequences
(`isSymm`, `posDef`, `det`, `discriminant form values`) read off through the
generic lemmas of `SRG266/Lattice/RootLattice.lean`.

`det E₆ = 3`, `det E₇ = 2`, `det E₈ = 1`: `E₈` is unimodular, so its discriminant group is trivial.
-/

namespace SRG266
namespace Lattice

open RootData

set_option maxRecDepth 40000


/-! ### `E6` -/

/-- The kernel certificate of the root lattice `E6`. -/
def certE6 : RootLatticeCert 6 where
  gram := gramE 6
  ldlt := matrixOfData e6LdltFactorData
  weight := vectorOfData e6LdltWeightData
  scale := e6LdltScale
  det := e6Det
  adjugate := matrixOfData e6AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `E6`. -/
theorem det_e6 : (gramE 6).det = e6Det := certE6.det_gram

/-- The closed formula `ADEType.det` is right at `E6`. -/
theorem det_gram_e6 : (ADEType.E6).gram.det = (ADEType.E6).det := by
  show (gramE 6).det = ADEType.det (ADEType.E6)
  rw [det_e6]
  decide

/-- The Cartan form of `E6` is positive definite. -/
theorem posDef_e6 : ∀ v : Fin 6 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramE 6) v v :=
  certE6.posDef

/-- Every listed coset minimum of `E6` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_e6 : ∀ t < e6CosetData.size,
    CosetMinAgrees 6 e6AdjugateData e6Det e6CosetData e6CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `E7` -/

/-- The kernel certificate of the root lattice `E7`. -/
def certE7 : RootLatticeCert 7 where
  gram := gramE 7
  ldlt := matrixOfData e7LdltFactorData
  weight := vectorOfData e7LdltWeightData
  scale := e7LdltScale
  det := e7Det
  adjugate := matrixOfData e7AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `E7`. -/
theorem det_e7 : (gramE 7).det = e7Det := certE7.det_gram

/-- The closed formula `ADEType.det` is right at `E7`. -/
theorem det_gram_e7 : (ADEType.E7).gram.det = (ADEType.E7).det := by
  show (gramE 7).det = ADEType.det (ADEType.E7)
  rw [det_e7]
  decide

/-- The Cartan form of `E7` is positive definite. -/
theorem posDef_e7 : ∀ v : Fin 7 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramE 7) v v :=
  certE7.posDef

/-- Every listed coset minimum of `E7` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_e7 : ∀ t < e7CosetData.size,
    CosetMinAgrees 7 e7AdjugateData e7Det e7CosetData e7CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `E8` -/

/-- The kernel certificate of the root lattice `E8`. -/
def certE8 : RootLatticeCert 8 where
  gram := gramE 8
  ldlt := matrixOfData e8LdltFactorData
  weight := vectorOfData e8LdltWeightData
  scale := e8LdltScale
  det := e8Det
  adjugate := matrixOfData e8AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `E8`. -/
theorem det_e8 : (gramE 8).det = e8Det := certE8.det_gram

/-- The closed formula `ADEType.det` is right at `E8`. -/
theorem det_gram_e8 : (ADEType.E8).gram.det = (ADEType.E8).det := by
  show (gramE 8).det = ADEType.det (ADEType.E8)
  rw [det_e8]
  decide

/-- The Cartan form of `E8` is positive definite. -/
theorem posDef_e8 : ∀ v : Fin 8 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramE 8) v v :=
  certE8.posDef

/-- Every listed coset minimum of `E8` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_e8 : ∀ t < e8CosetData.size,
    CosetMinAgrees 8 e8AdjugateData e8Det e8CosetData e8CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

end Lattice
end SRG266
