/-
Copyright (c) 2026.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: SRG266 formalization contributors

Generated by `scripts/generate_root_lattice_data.py`.
Do not edit by hand.
-/
import SRG266.Lattice.RootLattice
import SRG266.Certificates.RootLatticeData

/-!
# Kernel certificates for the irreducible root lattices of type `Dₙ`

One `SRG266.Lattice.RootLatticeCert` per type, each field checked by
`decide +kernel` against the generated data of
`SRG266/Certificates/RootLatticeData.lean`, and the four consequences
(`isSymm`, `posDef`, `det`, `discriminant form values`) read off through the
generic lemmas of `SRG266/Lattice/RootLattice.lean`.

`det Dₙ = 4`; the three non-zero cosets are the vector class and the two spinor classes, of minima `1`, `n/4`, `n/4`.
-/

namespace SRG266
namespace Lattice

open RootData

set_option maxRecDepth 40000


/-! ### `D4` -/

/-- The kernel certificate of the root lattice `D4`. -/
def certD4 : RootLatticeCert 4 where
  gram := gramD 4
  ldlt := matrixOfData d4LdltFactorData
  weight := vectorOfData d4LdltWeightData
  scale := d4LdltScale
  det := d4Det
  adjugate := matrixOfData d4AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D4`. -/
theorem det_d4 : (gramD 4).det = d4Det := certD4.det_gram

/-- The closed formula `ADEType.det` is right at `D4`. -/
theorem det_gram_d4 : (ADEType.D 4).gram.det = (ADEType.D 4).det := by
  show (gramD 4).det = ADEType.det (ADEType.D 4)
  rw [det_d4]
  decide

/-- The Cartan form of `D4` is positive definite. -/
theorem posDef_d4 : ∀ v : Fin 4 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 4) v v :=
  certD4.posDef

/-- Every listed coset minimum of `D4` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d4 : ∀ t < d4CosetData.size,
    CosetMinAgrees 4 d4AdjugateData d4Det d4CosetData d4CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `D5` -/

/-- The kernel certificate of the root lattice `D5`. -/
def certD5 : RootLatticeCert 5 where
  gram := gramD 5
  ldlt := matrixOfData d5LdltFactorData
  weight := vectorOfData d5LdltWeightData
  scale := d5LdltScale
  det := d5Det
  adjugate := matrixOfData d5AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D5`. -/
theorem det_d5 : (gramD 5).det = d5Det := certD5.det_gram

/-- The closed formula `ADEType.det` is right at `D5`. -/
theorem det_gram_d5 : (ADEType.D 5).gram.det = (ADEType.D 5).det := by
  show (gramD 5).det = ADEType.det (ADEType.D 5)
  rw [det_d5]
  decide

/-- The Cartan form of `D5` is positive definite. -/
theorem posDef_d5 : ∀ v : Fin 5 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 5) v v :=
  certD5.posDef

/-- Every listed coset minimum of `D5` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d5 : ∀ t < d5CosetData.size,
    CosetMinAgrees 5 d5AdjugateData d5Det d5CosetData d5CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `D6` -/

/-- The kernel certificate of the root lattice `D6`. -/
def certD6 : RootLatticeCert 6 where
  gram := gramD 6
  ldlt := matrixOfData d6LdltFactorData
  weight := vectorOfData d6LdltWeightData
  scale := d6LdltScale
  det := d6Det
  adjugate := matrixOfData d6AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D6`. -/
theorem det_d6 : (gramD 6).det = d6Det := certD6.det_gram

/-- The closed formula `ADEType.det` is right at `D6`. -/
theorem det_gram_d6 : (ADEType.D 6).gram.det = (ADEType.D 6).det := by
  show (gramD 6).det = ADEType.det (ADEType.D 6)
  rw [det_d6]
  decide

/-- The Cartan form of `D6` is positive definite. -/
theorem posDef_d6 : ∀ v : Fin 6 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 6) v v :=
  certD6.posDef

/-- Every listed coset minimum of `D6` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d6 : ∀ t < d6CosetData.size,
    CosetMinAgrees 6 d6AdjugateData d6Det d6CosetData d6CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `D7` -/

/-- The kernel certificate of the root lattice `D7`. -/
def certD7 : RootLatticeCert 7 where
  gram := gramD 7
  ldlt := matrixOfData d7LdltFactorData
  weight := vectorOfData d7LdltWeightData
  scale := d7LdltScale
  det := d7Det
  adjugate := matrixOfData d7AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D7`. -/
theorem det_d7 : (gramD 7).det = d7Det := certD7.det_gram

/-- The closed formula `ADEType.det` is right at `D7`. -/
theorem det_gram_d7 : (ADEType.D 7).gram.det = (ADEType.D 7).det := by
  show (gramD 7).det = ADEType.det (ADEType.D 7)
  rw [det_d7]
  decide

/-- The Cartan form of `D7` is positive definite. -/
theorem posDef_d7 : ∀ v : Fin 7 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 7) v v :=
  certD7.posDef

/-- Every listed coset minimum of `D7` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d7 : ∀ t < d7CosetData.size,
    CosetMinAgrees 7 d7AdjugateData d7Det d7CosetData d7CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `D8` -/

/-- The kernel certificate of the root lattice `D8`. -/
def certD8 : RootLatticeCert 8 where
  gram := gramD 8
  ldlt := matrixOfData d8LdltFactorData
  weight := vectorOfData d8LdltWeightData
  scale := d8LdltScale
  det := d8Det
  adjugate := matrixOfData d8AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D8`. -/
theorem det_d8 : (gramD 8).det = d8Det := certD8.det_gram

/-- The closed formula `ADEType.det` is right at `D8`. -/
theorem det_gram_d8 : (ADEType.D 8).gram.det = (ADEType.D 8).det := by
  show (gramD 8).det = ADEType.det (ADEType.D 8)
  rw [det_d8]
  decide

/-- The Cartan form of `D8` is positive definite. -/
theorem posDef_d8 : ∀ v : Fin 8 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 8) v v :=
  certD8.posDef

/-- Every listed coset minimum of `D8` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d8 : ∀ t < d8CosetData.size,
    CosetMinAgrees 8 d8AdjugateData d8Det d8CosetData d8CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `D9` -/

/-- The kernel certificate of the root lattice `D9`. -/
def certD9 : RootLatticeCert 9 where
  gram := gramD 9
  ldlt := matrixOfData d9LdltFactorData
  weight := vectorOfData d9LdltWeightData
  scale := d9LdltScale
  det := d9Det
  adjugate := matrixOfData d9AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D9`. -/
theorem det_d9 : (gramD 9).det = d9Det := certD9.det_gram

/-- The closed formula `ADEType.det` is right at `D9`. -/
theorem det_gram_d9 : (ADEType.D 9).gram.det = (ADEType.D 9).det := by
  show (gramD 9).det = ADEType.det (ADEType.D 9)
  rw [det_d9]
  decide

/-- The Cartan form of `D9` is positive definite. -/
theorem posDef_d9 : ∀ v : Fin 9 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 9) v v :=
  certD9.posDef

/-- Every listed coset minimum of `D9` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d9 : ∀ t < d9CosetData.size,
    CosetMinAgrees 9 d9AdjugateData d9Det d9CosetData d9CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `D10` -/

/-- The kernel certificate of the root lattice `D10`. -/
def certD10 : RootLatticeCert 10 where
  gram := gramD 10
  ldlt := matrixOfData d10LdltFactorData
  weight := vectorOfData d10LdltWeightData
  scale := d10LdltScale
  det := d10Det
  adjugate := matrixOfData d10AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D10`. -/
theorem det_d10 : (gramD 10).det = d10Det := certD10.det_gram

/-- The closed formula `ADEType.det` is right at `D10`. -/
theorem det_gram_d10 : (ADEType.D 10).gram.det = (ADEType.D 10).det := by
  show (gramD 10).det = ADEType.det (ADEType.D 10)
  rw [det_d10]
  decide

/-- The Cartan form of `D10` is positive definite. -/
theorem posDef_d10 : ∀ v : Fin 10 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 10) v v :=
  certD10.posDef

/-- Every listed coset minimum of `D10` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d10 : ∀ t < d10CosetData.size,
    CosetMinAgrees 10 d10AdjugateData d10Det d10CosetData d10CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `D11` -/

/-- The kernel certificate of the root lattice `D11`. -/
def certD11 : RootLatticeCert 11 where
  gram := gramD 11
  ldlt := matrixOfData d11LdltFactorData
  weight := vectorOfData d11LdltWeightData
  scale := d11LdltScale
  det := d11Det
  adjugate := matrixOfData d11AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D11`. -/
theorem det_d11 : (gramD 11).det = d11Det := certD11.det_gram

/-- The closed formula `ADEType.det` is right at `D11`. -/
theorem det_gram_d11 : (ADEType.D 11).gram.det = (ADEType.D 11).det := by
  show (gramD 11).det = ADEType.det (ADEType.D 11)
  rw [det_d11]
  decide

/-- The Cartan form of `D11` is positive definite. -/
theorem posDef_d11 : ∀ v : Fin 11 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 11) v v :=
  certD11.posDef

/-- Every listed coset minimum of `D11` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d11 : ∀ t < d11CosetData.size,
    CosetMinAgrees 11 d11AdjugateData d11Det d11CosetData d11CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `D12` -/

/-- The kernel certificate of the root lattice `D12`. -/
def certD12 : RootLatticeCert 12 where
  gram := gramD 12
  ldlt := matrixOfData d12LdltFactorData
  weight := vectorOfData d12LdltWeightData
  scale := d12LdltScale
  det := d12Det
  adjugate := matrixOfData d12AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D12`. -/
theorem det_d12 : (gramD 12).det = d12Det := certD12.det_gram

/-- The closed formula `ADEType.det` is right at `D12`. -/
theorem det_gram_d12 : (ADEType.D 12).gram.det = (ADEType.D 12).det := by
  show (gramD 12).det = ADEType.det (ADEType.D 12)
  rw [det_d12]
  decide

/-- The Cartan form of `D12` is positive definite. -/
theorem posDef_d12 : ∀ v : Fin 12 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 12) v v :=
  certD12.posDef

/-- Every listed coset minimum of `D12` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d12 : ∀ t < d12CosetData.size,
    CosetMinAgrees 12 d12AdjugateData d12Det d12CosetData d12CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `D13` -/

/-- The kernel certificate of the root lattice `D13`. -/
def certD13 : RootLatticeCert 13 where
  gram := gramD 13
  ldlt := matrixOfData d13LdltFactorData
  weight := vectorOfData d13LdltWeightData
  scale := d13LdltScale
  det := d13Det
  adjugate := matrixOfData d13AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D13`. -/
theorem det_d13 : (gramD 13).det = d13Det := certD13.det_gram

/-- The closed formula `ADEType.det` is right at `D13`. -/
theorem det_gram_d13 : (ADEType.D 13).gram.det = (ADEType.D 13).det := by
  show (gramD 13).det = ADEType.det (ADEType.D 13)
  rw [det_d13]
  decide

/-- The Cartan form of `D13` is positive definite. -/
theorem posDef_d13 : ∀ v : Fin 13 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 13) v v :=
  certD13.posDef

/-- Every listed coset minimum of `D13` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d13 : ∀ t < d13CosetData.size,
    CosetMinAgrees 13 d13AdjugateData d13Det d13CosetData d13CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `D14` -/

/-- The kernel certificate of the root lattice `D14`. -/
def certD14 : RootLatticeCert 14 where
  gram := gramD 14
  ldlt := matrixOfData d14LdltFactorData
  weight := vectorOfData d14LdltWeightData
  scale := d14LdltScale
  det := d14Det
  adjugate := matrixOfData d14AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D14`. -/
theorem det_d14 : (gramD 14).det = d14Det := certD14.det_gram

/-- The closed formula `ADEType.det` is right at `D14`. -/
theorem det_gram_d14 : (ADEType.D 14).gram.det = (ADEType.D 14).det := by
  show (gramD 14).det = ADEType.det (ADEType.D 14)
  rw [det_d14]
  decide

/-- The Cartan form of `D14` is positive definite. -/
theorem posDef_d14 : ∀ v : Fin 14 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 14) v v :=
  certD14.posDef

/-- Every listed coset minimum of `D14` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d14 : ∀ t < d14CosetData.size,
    CosetMinAgrees 14 d14AdjugateData d14Det d14CosetData d14CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `D15` -/

/-- The kernel certificate of the root lattice `D15`. -/
def certD15 : RootLatticeCert 15 where
  gram := gramD 15
  ldlt := matrixOfData d15LdltFactorData
  weight := vectorOfData d15LdltWeightData
  scale := d15LdltScale
  det := d15Det
  adjugate := matrixOfData d15AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `D15`. -/
theorem det_d15 : (gramD 15).det = d15Det := certD15.det_gram

/-- The closed formula `ADEType.det` is right at `D15`. -/
theorem det_gram_d15 : (ADEType.D 15).gram.det = (ADEType.D 15).det := by
  show (gramD 15).det = ADEType.det (ADEType.D 15)
  rw [det_d15]
  decide

/-- The Cartan form of `D15` is positive definite. -/
theorem posDef_d15 : ∀ v : Fin 15 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramD 15) v v :=
  certD15.posDef

/-- Every listed coset minimum of `D15` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_d15 : ∀ t < d15CosetData.size,
    CosetMinAgrees 15 d15AdjugateData d15Det d15CosetData d15CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

end Lattice
end SRG266
