/-
Copyright (c) 2026 Kay Akiyama.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Kay Akiyama

Generated by `scripts/generate_root_lattice_data.py`.
Do not edit by hand.
-/
import SRG266.Lattice.RootLattice
import SRG266.Certificates.RootLatticeData

/-!
# Kernel certificates for the irreducible root lattices of type `Aₙ`

One `SRG266.Lattice.RootLatticeCert` per type, each field checked by
`decide +kernel` against the generated data of
`SRG266/Certificates/RootLatticeData.lean`, and the four consequences
(`isSymm`, `posDef`, `det`, `discriminant form values`) read off through the
generic lemmas of `SRG266/Lattice/RootLattice.lean`.

`det Aₙ = n + 1`, and the discriminant group is cyclic of that order, generated by the first dual basis vector.
-/

namespace SRG266
namespace Lattice

open RootData

set_option maxRecDepth 40000


/-! ### `A1` -/

/-- The kernel certificate of the root lattice `A1`. -/
def certA1 : RootLatticeCert 1 where
  gram := gramA 1
  ldlt := matrixOfData a1LdltFactorData
  weight := vectorOfData a1LdltWeightData
  scale := a1LdltScale
  det := a1Det
  adjugate := matrixOfData a1AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A1`. -/
theorem det_a1 : (gramA 1).det = a1Det := certA1.det_gram

/-- The closed formula `ADEType.det` is right at `A1`. -/
theorem det_gram_a1 : (ADEType.A 1).gram.det = (ADEType.A 1).det := by
  show (gramA 1).det = ADEType.det (ADEType.A 1)
  rw [det_a1]
  decide

/-- The Cartan form of `A1` is positive definite. -/
theorem posDef_a1 : ∀ v : Fin 1 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 1) v v :=
  certA1.posDef

/-- Every listed coset minimum of `A1` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a1 : ∀ t < a1CosetData.size,
    CosetMinAgrees 1 a1AdjugateData a1Det a1CosetData a1CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A2` -/

/-- The kernel certificate of the root lattice `A2`. -/
def certA2 : RootLatticeCert 2 where
  gram := gramA 2
  ldlt := matrixOfData a2LdltFactorData
  weight := vectorOfData a2LdltWeightData
  scale := a2LdltScale
  det := a2Det
  adjugate := matrixOfData a2AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A2`. -/
theorem det_a2 : (gramA 2).det = a2Det := certA2.det_gram

/-- The closed formula `ADEType.det` is right at `A2`. -/
theorem det_gram_a2 : (ADEType.A 2).gram.det = (ADEType.A 2).det := by
  show (gramA 2).det = ADEType.det (ADEType.A 2)
  rw [det_a2]
  decide

/-- The Cartan form of `A2` is positive definite. -/
theorem posDef_a2 : ∀ v : Fin 2 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 2) v v :=
  certA2.posDef

/-- Every listed coset minimum of `A2` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a2 : ∀ t < a2CosetData.size,
    CosetMinAgrees 2 a2AdjugateData a2Det a2CosetData a2CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A3` -/

/-- The kernel certificate of the root lattice `A3`. -/
def certA3 : RootLatticeCert 3 where
  gram := gramA 3
  ldlt := matrixOfData a3LdltFactorData
  weight := vectorOfData a3LdltWeightData
  scale := a3LdltScale
  det := a3Det
  adjugate := matrixOfData a3AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A3`. -/
theorem det_a3 : (gramA 3).det = a3Det := certA3.det_gram

/-- The closed formula `ADEType.det` is right at `A3`. -/
theorem det_gram_a3 : (ADEType.A 3).gram.det = (ADEType.A 3).det := by
  show (gramA 3).det = ADEType.det (ADEType.A 3)
  rw [det_a3]
  decide

/-- The Cartan form of `A3` is positive definite. -/
theorem posDef_a3 : ∀ v : Fin 3 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 3) v v :=
  certA3.posDef

/-- Every listed coset minimum of `A3` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a3 : ∀ t < a3CosetData.size,
    CosetMinAgrees 3 a3AdjugateData a3Det a3CosetData a3CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A4` -/

/-- The kernel certificate of the root lattice `A4`. -/
def certA4 : RootLatticeCert 4 where
  gram := gramA 4
  ldlt := matrixOfData a4LdltFactorData
  weight := vectorOfData a4LdltWeightData
  scale := a4LdltScale
  det := a4Det
  adjugate := matrixOfData a4AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A4`. -/
theorem det_a4 : (gramA 4).det = a4Det := certA4.det_gram

/-- The closed formula `ADEType.det` is right at `A4`. -/
theorem det_gram_a4 : (ADEType.A 4).gram.det = (ADEType.A 4).det := by
  show (gramA 4).det = ADEType.det (ADEType.A 4)
  rw [det_a4]
  decide

/-- The Cartan form of `A4` is positive definite. -/
theorem posDef_a4 : ∀ v : Fin 4 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 4) v v :=
  certA4.posDef

/-- Every listed coset minimum of `A4` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a4 : ∀ t < a4CosetData.size,
    CosetMinAgrees 4 a4AdjugateData a4Det a4CosetData a4CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A5` -/

/-- The kernel certificate of the root lattice `A5`. -/
def certA5 : RootLatticeCert 5 where
  gram := gramA 5
  ldlt := matrixOfData a5LdltFactorData
  weight := vectorOfData a5LdltWeightData
  scale := a5LdltScale
  det := a5Det
  adjugate := matrixOfData a5AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A5`. -/
theorem det_a5 : (gramA 5).det = a5Det := certA5.det_gram

/-- The closed formula `ADEType.det` is right at `A5`. -/
theorem det_gram_a5 : (ADEType.A 5).gram.det = (ADEType.A 5).det := by
  show (gramA 5).det = ADEType.det (ADEType.A 5)
  rw [det_a5]
  decide

/-- The Cartan form of `A5` is positive definite. -/
theorem posDef_a5 : ∀ v : Fin 5 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 5) v v :=
  certA5.posDef

/-- Every listed coset minimum of `A5` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a5 : ∀ t < a5CosetData.size,
    CosetMinAgrees 5 a5AdjugateData a5Det a5CosetData a5CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A6` -/

/-- The kernel certificate of the root lattice `A6`. -/
def certA6 : RootLatticeCert 6 where
  gram := gramA 6
  ldlt := matrixOfData a6LdltFactorData
  weight := vectorOfData a6LdltWeightData
  scale := a6LdltScale
  det := a6Det
  adjugate := matrixOfData a6AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A6`. -/
theorem det_a6 : (gramA 6).det = a6Det := certA6.det_gram

/-- The closed formula `ADEType.det` is right at `A6`. -/
theorem det_gram_a6 : (ADEType.A 6).gram.det = (ADEType.A 6).det := by
  show (gramA 6).det = ADEType.det (ADEType.A 6)
  rw [det_a6]
  decide

/-- The Cartan form of `A6` is positive definite. -/
theorem posDef_a6 : ∀ v : Fin 6 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 6) v v :=
  certA6.posDef

/-- Every listed coset minimum of `A6` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a6 : ∀ t < a6CosetData.size,
    CosetMinAgrees 6 a6AdjugateData a6Det a6CosetData a6CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A7` -/

/-- The kernel certificate of the root lattice `A7`. -/
def certA7 : RootLatticeCert 7 where
  gram := gramA 7
  ldlt := matrixOfData a7LdltFactorData
  weight := vectorOfData a7LdltWeightData
  scale := a7LdltScale
  det := a7Det
  adjugate := matrixOfData a7AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A7`. -/
theorem det_a7 : (gramA 7).det = a7Det := certA7.det_gram

/-- The closed formula `ADEType.det` is right at `A7`. -/
theorem det_gram_a7 : (ADEType.A 7).gram.det = (ADEType.A 7).det := by
  show (gramA 7).det = ADEType.det (ADEType.A 7)
  rw [det_a7]
  decide

/-- The Cartan form of `A7` is positive definite. -/
theorem posDef_a7 : ∀ v : Fin 7 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 7) v v :=
  certA7.posDef

/-- Every listed coset minimum of `A7` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a7 : ∀ t < a7CosetData.size,
    CosetMinAgrees 7 a7AdjugateData a7Det a7CosetData a7CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A8` -/

/-- The kernel certificate of the root lattice `A8`. -/
def certA8 : RootLatticeCert 8 where
  gram := gramA 8
  ldlt := matrixOfData a8LdltFactorData
  weight := vectorOfData a8LdltWeightData
  scale := a8LdltScale
  det := a8Det
  adjugate := matrixOfData a8AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A8`. -/
theorem det_a8 : (gramA 8).det = a8Det := certA8.det_gram

/-- The closed formula `ADEType.det` is right at `A8`. -/
theorem det_gram_a8 : (ADEType.A 8).gram.det = (ADEType.A 8).det := by
  show (gramA 8).det = ADEType.det (ADEType.A 8)
  rw [det_a8]
  decide

/-- The Cartan form of `A8` is positive definite. -/
theorem posDef_a8 : ∀ v : Fin 8 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 8) v v :=
  certA8.posDef

/-- Every listed coset minimum of `A8` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a8 : ∀ t < a8CosetData.size,
    CosetMinAgrees 8 a8AdjugateData a8Det a8CosetData a8CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A9` -/

/-- The kernel certificate of the root lattice `A9`. -/
def certA9 : RootLatticeCert 9 where
  gram := gramA 9
  ldlt := matrixOfData a9LdltFactorData
  weight := vectorOfData a9LdltWeightData
  scale := a9LdltScale
  det := a9Det
  adjugate := matrixOfData a9AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A9`. -/
theorem det_a9 : (gramA 9).det = a9Det := certA9.det_gram

/-- The closed formula `ADEType.det` is right at `A9`. -/
theorem det_gram_a9 : (ADEType.A 9).gram.det = (ADEType.A 9).det := by
  show (gramA 9).det = ADEType.det (ADEType.A 9)
  rw [det_a9]
  decide

/-- The Cartan form of `A9` is positive definite. -/
theorem posDef_a9 : ∀ v : Fin 9 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 9) v v :=
  certA9.posDef

/-- Every listed coset minimum of `A9` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a9 : ∀ t < a9CosetData.size,
    CosetMinAgrees 9 a9AdjugateData a9Det a9CosetData a9CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A10` -/

/-- The kernel certificate of the root lattice `A10`. -/
def certA10 : RootLatticeCert 10 where
  gram := gramA 10
  ldlt := matrixOfData a10LdltFactorData
  weight := vectorOfData a10LdltWeightData
  scale := a10LdltScale
  det := a10Det
  adjugate := matrixOfData a10AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A10`. -/
theorem det_a10 : (gramA 10).det = a10Det := certA10.det_gram

/-- The closed formula `ADEType.det` is right at `A10`. -/
theorem det_gram_a10 : (ADEType.A 10).gram.det = (ADEType.A 10).det := by
  show (gramA 10).det = ADEType.det (ADEType.A 10)
  rw [det_a10]
  decide

/-- The Cartan form of `A10` is positive definite. -/
theorem posDef_a10 : ∀ v : Fin 10 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 10) v v :=
  certA10.posDef

/-- Every listed coset minimum of `A10` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a10 : ∀ t < a10CosetData.size,
    CosetMinAgrees 10 a10AdjugateData a10Det a10CosetData a10CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A11` -/

/-- The kernel certificate of the root lattice `A11`. -/
def certA11 : RootLatticeCert 11 where
  gram := gramA 11
  ldlt := matrixOfData a11LdltFactorData
  weight := vectorOfData a11LdltWeightData
  scale := a11LdltScale
  det := a11Det
  adjugate := matrixOfData a11AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A11`. -/
theorem det_a11 : (gramA 11).det = a11Det := certA11.det_gram

/-- The closed formula `ADEType.det` is right at `A11`. -/
theorem det_gram_a11 : (ADEType.A 11).gram.det = (ADEType.A 11).det := by
  show (gramA 11).det = ADEType.det (ADEType.A 11)
  rw [det_a11]
  decide

/-- The Cartan form of `A11` is positive definite. -/
theorem posDef_a11 : ∀ v : Fin 11 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 11) v v :=
  certA11.posDef

/-- Every listed coset minimum of `A11` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a11 : ∀ t < a11CosetData.size,
    CosetMinAgrees 11 a11AdjugateData a11Det a11CosetData a11CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A12` -/

/-- The kernel certificate of the root lattice `A12`. -/
def certA12 : RootLatticeCert 12 where
  gram := gramA 12
  ldlt := matrixOfData a12LdltFactorData
  weight := vectorOfData a12LdltWeightData
  scale := a12LdltScale
  det := a12Det
  adjugate := matrixOfData a12AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A12`. -/
theorem det_a12 : (gramA 12).det = a12Det := certA12.det_gram

/-- The closed formula `ADEType.det` is right at `A12`. -/
theorem det_gram_a12 : (ADEType.A 12).gram.det = (ADEType.A 12).det := by
  show (gramA 12).det = ADEType.det (ADEType.A 12)
  rw [det_a12]
  decide

/-- The Cartan form of `A12` is positive definite. -/
theorem posDef_a12 : ∀ v : Fin 12 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 12) v v :=
  certA12.posDef

/-- Every listed coset minimum of `A12` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a12 : ∀ t < a12CosetData.size,
    CosetMinAgrees 12 a12AdjugateData a12Det a12CosetData a12CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A13` -/

/-- The kernel certificate of the root lattice `A13`. -/
def certA13 : RootLatticeCert 13 where
  gram := gramA 13
  ldlt := matrixOfData a13LdltFactorData
  weight := vectorOfData a13LdltWeightData
  scale := a13LdltScale
  det := a13Det
  adjugate := matrixOfData a13AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A13`. -/
theorem det_a13 : (gramA 13).det = a13Det := certA13.det_gram

/-- The closed formula `ADEType.det` is right at `A13`. -/
theorem det_gram_a13 : (ADEType.A 13).gram.det = (ADEType.A 13).det := by
  show (gramA 13).det = ADEType.det (ADEType.A 13)
  rw [det_a13]
  decide

/-- The Cartan form of `A13` is positive definite. -/
theorem posDef_a13 : ∀ v : Fin 13 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 13) v v :=
  certA13.posDef

/-- Every listed coset minimum of `A13` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a13 : ∀ t < a13CosetData.size,
    CosetMinAgrees 13 a13AdjugateData a13Det a13CosetData a13CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A14` -/

/-- The kernel certificate of the root lattice `A14`. -/
def certA14 : RootLatticeCert 14 where
  gram := gramA 14
  ldlt := matrixOfData a14LdltFactorData
  weight := vectorOfData a14LdltWeightData
  scale := a14LdltScale
  det := a14Det
  adjugate := matrixOfData a14AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A14`. -/
theorem det_a14 : (gramA 14).det = a14Det := certA14.det_gram

/-- The closed formula `ADEType.det` is right at `A14`. -/
theorem det_gram_a14 : (ADEType.A 14).gram.det = (ADEType.A 14).det := by
  show (gramA 14).det = ADEType.det (ADEType.A 14)
  rw [det_a14]
  decide

/-- The Cartan form of `A14` is positive definite. -/
theorem posDef_a14 : ∀ v : Fin 14 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 14) v v :=
  certA14.posDef

/-- Every listed coset minimum of `A14` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a14 : ∀ t < a14CosetData.size,
    CosetMinAgrees 14 a14AdjugateData a14Det a14CosetData a14CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

/-! ### `A15` -/

/-- The kernel certificate of the root lattice `A15`. -/
def certA15 : RootLatticeCert 15 where
  gram := gramA 15
  ldlt := matrixOfData a15LdltFactorData
  weight := vectorOfData a15LdltWeightData
  scale := a15LdltScale
  det := a15Det
  adjugate := matrixOfData a15AdjugateData
  symm := by decide +kernel
  lower := by decide +kernel
  ldlt_check := by decide +kernel
  scale_pos := by decide
  det_pos := by decide
  adjugate_check := by decide +kernel
  det_check := by decide +kernel

/-- `det` of the Cartan matrix of `A15`. -/
theorem det_a15 : (gramA 15).det = a15Det := certA15.det_gram

/-- The closed formula `ADEType.det` is right at `A15`. -/
theorem det_gram_a15 : (ADEType.A 15).gram.det = (ADEType.A 15).det := by
  show (gramA 15).det = ADEType.det (ADEType.A 15)
  rw [det_a15]
  decide

/-- The Cartan form of `A15` is positive definite. -/
theorem posDef_a15 : ∀ v : Fin 15 → ℤ, v ≠ 0 → 0 < Matrix.toBilin' (gramA 15) v v :=
  certA15.posDef

/-- Every listed coset minimum of `A15` agrees with the discriminant form
value of the listed representative modulo `2ℤ`. -/
theorem cosetMin_a15 : ∀ t < a15CosetData.size,
    CosetMinAgrees 15 a15AdjugateData a15Det a15CosetData a15CosetMinData t :=
  cosetMinAgrees_of_check (by decide +kernel)

end Lattice
end SRG266
